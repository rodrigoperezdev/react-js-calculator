import React from "react";
import ReactDOM from "react-dom/client";
import { ReactCalculator } from "./pages/ReactCalculator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ReactCalculator />
  </React.StrictMode>
);
